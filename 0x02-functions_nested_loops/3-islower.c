#include "main.h"
/**
* _islower - checks for alphanumerical letters
* @c: a character to be checked on
* Description: for _islower
* Return: returns 0 or 1 depending on condition
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
