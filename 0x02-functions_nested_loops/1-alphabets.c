#include "main.h"
/**
* main - check the code
* Description: prints alphabets from a to z in lowercase
* Return: always 0.
*/
void print_alphabet(void)
{
int a;
for (a = 'a'; a <= 'z'; a++)
{
_putchar(a);
}
_putchar('\n');
}
